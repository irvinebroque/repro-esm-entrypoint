{
  "name": "repro-esm-entrypoint",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "repro:wrangler": "wrangler deploy --config wrangler.jsonc --dry-run --outdir dist/wrangler",
    "repro:vite": "vite build && node scripts/check-vite-output.mjs",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.53.1",
    "typescript": "^5.5.2",
    "vite": "^8.2.2",
    "wrangler": "^4.125.0"
  }
}
